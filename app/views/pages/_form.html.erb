<%= error_messages_for(@page) %>
 <table>
    	<tr>
    		<th>Name</th>
    		<td><%= f.text_field(:name) %></td>
    	</tr>
    	<tr>
    		<th><%= f.label(:position) %></th>
    		<td><%= f.number_field(:position) %></td>
    	</tr>
    	<tr>
    		<th>Visible</th>
    		<td><%= f.check_box(:visible) %></td>
    	</tr>
    	<tr>
    		<th>Permalink</th>
    		<td><%= f.text_field(:permalink) %></td>
    	</tr>
    	<tr>
    		<th><%= f.label(:subject_id, "subject ID") %></th>
    		<td><%= f.select :subject_id, options_for_select(Subject.all.collect{|s| [s.name, s.id]}) %></td>
    	</tr>

    </table>